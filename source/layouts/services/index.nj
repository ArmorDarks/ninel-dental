{% extends "_layout.nj" %}


{# --------- #}
{% set pageTitle         = getPage(pages, page.breadcrumb).name %}
{% set pageMetaDesc      = getPage(pages, page.breadcrumb).metaDesc %}
{% set pageContentTitle  = pageTitle %}

{% set pageClass         = "l-other" %}

{% set showBreadcrumb    = true %}
{% set breadcrumb        = page.breadcrumb %}
{# --------- #}


{% block main %}

	<p>Клиника художественной реставрации «Нинель» предоставляет следующие услуги:</p>

	<div id="other_serv">

		<ul class="level_0">
			{% for slug, entry in getPage(pages, page.breadcrumb).sub %}
			<li>
				<a href="{{ slug }}">{{ entry.name }}</a>
				<span class="ul_under">{{ entry.descShort }}</span>
			</li>
			{% endfor %}
		</ul>

	</div>

{% endblock %}